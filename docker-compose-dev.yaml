version: '3'
services:
  webserver:
    container_name: producer
    image: rust-skeleton:latest
    entrypoint: /webserver
    volumes:
      - ./config:/config
    networks:
      network:

networks:
  network:
    ipam:
      driver: default
      config:
        - subnet: 172.24.125.0/16
